---
name: tokens
description: Run the token audit — rebuild tokens.json from Figma and write Base + Semantic tables to Airtable.
---

# /tokens

Delegate to the **token-audit** agent. Diff Figma against `tokens.json`, rebuild if changed,
write Base + Semantic tables, and return the audit card.
