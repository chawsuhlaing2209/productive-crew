---
name: parity
description: Run the token parity check — Figma vs local tokens/ — and write Parity Status to both tables.
---

# /parity

Delegate to the **token-parity** agent. Compare Figma and local `tokens/`, write Parity Status
(passed / failed / in testing) to both tables, and list any mismatches by token name.
